# Pinned: an unpinned build is not reproducible, and SQLAlchemy 2.1 changed the
# default driver for `postgresql://` from psycopg2 to psycopg — which broke a
# deploy built from the same requirements file a day later.
requests==2.34.2
PyNaCl==1.6.2
SQLAlchemy==2.1.0
alembic==1.20.0
psycopg[binary]==3.3.6
python-dotenv==1.2.3
